interface:
  display_name: "Punkt B Sales CRM"
  short_description: "amoCRM, Umnico and GetCourse workflows"
  default_prompt: "Use $punktb-sales-crm to safely handle this Punkt B sales workflow."

dependencies:
  tools:
    - type: "mcp"
      value: "amocrm"
      description: "amoCRM and Umnico operations"
    - type: "mcp"
      value: "getcourse"
      description: "GetCourse API operations"

policy:
  allow_implicit_invocation: true
