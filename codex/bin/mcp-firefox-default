#!/usr/bin/env bash
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "$SCRIPT_DIR/mcp-firefox-devtools.sh" --capability firefox-default --binding-origin codex/bin/mcp-firefox-default --profile-key firefox-default --start-url http://127.0.0.1:8080/ "$@"
