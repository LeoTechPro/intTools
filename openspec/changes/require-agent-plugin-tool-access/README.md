# require-agent-plugin-tool-access

Require agents to use OpenSpec and Multica MCP plugin tools as the primary interface when available; direct CLI or repo-local wrappers are documented fallback/blocker paths only.
